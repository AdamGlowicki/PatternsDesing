package com.pattern.task;

import java.time.LocalDate;

public class Ford extends Car {

    public Ford(int volumeEngine, FuelType fuelType, LocalDate productionDate) {
        super(volumeEngine, fuelType, productionDate);
    }
}
