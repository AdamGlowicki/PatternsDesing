package com.pattern.task;

import java.time.LocalDate;

public class BMW extends Car {
    public BMW(int volumeEngine, FuelType fuelType, LocalDate productionDate) {
        super(volumeEngine, fuelType, productionDate);
    }
}
